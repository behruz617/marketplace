logging:
  level:
    root: ERROR              # yalnız WARN və yuxarı loglar çıxsın
    az.gov.marketplace: INFO  # sənin kodun isə INFO-dan yuxarı çıxsın
spring:


  security:
    jwt:
      secret: G8TlwANba6QV6bG9umVjG5LjFgdCxdM6rIIFaa+hvFQ=
      # açıq şəkildə saxlanır
      access-expiration: 3600000
      refresh-expiration: 604800000
  datasource:
    url: jdbc:postgresql://localhost:5432/marketdb
    username: market
    password: market
    jpa:
      hibernate:
        ddl-auto: validate   # sxemi Flyway idarə edəcək
      properties:
        hibernate.format_sql: true
        hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

springdoc:
  api-docs.path: /v3/api-docs
  swagger-ui.path: /swagger

